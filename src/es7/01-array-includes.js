// permite validar si en existe cierto elemento
// en un array
let numbers = [1, 3, 4, 5, 6, 7, 8];

console.log(numbers.includes(2));
console.log(numbers.includes(4));

const list = ['oscar','David','Ana'];
console.log(list.includes('oscar'));
console.log(list.includes('Oscar')); // false - por la O mayuscula