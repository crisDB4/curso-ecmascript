const array = ['one', 'two', 'three', 'four', 'five', 'six'];
// muchas veces no sabemos el tamaño del array (o las respuestas de la API)

//¿Como acceder al ultimo elemento del array sin saber cuantos elementos tiene?
console.log(array[array.length - 1]);

// acceder a la ultima posición
console.log(array.at(-1));
// de esta manera se accede a los elementos a partir del indice
